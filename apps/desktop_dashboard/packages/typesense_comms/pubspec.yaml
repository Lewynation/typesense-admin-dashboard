name: typesense_comms
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: "none"
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  # path: ^1.8.0
  typesense: ^0.3.0+1
  custom_logger:
    path: ../custom_logger

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
